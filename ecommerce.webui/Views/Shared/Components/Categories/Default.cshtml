@model List<Category>
 
@foreach (var item in Model)
{
    <li>
        <a asp-controller="Shop"
        asp-action="List"
        asp-route-category="@item.Url">
        @item.Name
        </a>
    </li>
}